<div class="list white"> Last created lists
	<ul id="list">
	<% @lists.each do |list| %>
		<li><a href="<%= list.slug %>" id="" ><%= list.title %></a></li>
	<% end %>
	</ul>
</div>  
<%= render 'form', list: @list %>
<div class="footer centered">
	<p class="white"><%= notice %></p>
	<h3 class="white">MANAGE YOUR OWN TASKS IN A DIFFERENT WAY<h3>
</div> 
<br>

